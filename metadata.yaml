ensemble: formula
name: jenkins
revision: 2
summary: Continuous integration server
description: |
  Jenkins is a continuous integration (CI) server.
provides:
  website:
    interface: http
  master:
    interface: jenkins
requires:
  slave:
    interface: jenkins
    optional: true
